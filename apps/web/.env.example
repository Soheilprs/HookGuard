# Browser-visible API origin. In production set this to the public API URL.
NEXT_PUBLIC_API_URL=http://localhost:3001
# Optional. Wallet connect is client-side only; HookGuard does not send transactions.
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=
